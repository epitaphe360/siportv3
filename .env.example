### Copy this file to `.env` and fill with your real Supabase values for local development.
# Never commit your real `.env` to the repo. The repository `.gitignore` already contains `.env`.
# SECURITY: Replace ALL placeholder values with your actual credentials

# URL to your Supabase project (must start with https://)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Public anon key (use the anon/public key from the Supabase dashboard)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# SECURITY: Service role key (DO NOT expose this to browsers). Only set in server-side environments.
# NEVER use VITE_ prefix for service role keys - they will be exposed in the client bundle
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Configuration Firebase pour l'authentification Google
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdefghijklmnop

# SECURITY: Generate strong random secrets for API authentication
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
EXHIBITORS_SECRET=generate_a_strong_random_secret_here
METRICS_SECRET=generate_a_strong_random_secret_here
JWT_SECRET=generate_a_strong_random_secret_here

# Optional custom port for the exhibitors server (defaults to 4002)
EXHIBITORS_PORT=4002

# Frontend access to exhibitors fallback server (optional)
VITE_EXHIBITORS_SERVER_URL=http://localhost:4002
VITE_EXHIBITORS_SECRET=same_as_EXHIBITORS_SECRET_above

# SECURITY: Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

