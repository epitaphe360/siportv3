### Copy this file to `.env` and fill with your real Supabase values for local development.
# Never commit your real `.env` to the repo. The repository `.gitignore` already contains `.env`.
#
# ⚠️ SECURITY WARNING: Never commit real credentials to this file!
# ⚠️ All values below are placeholders - replace with your actual credentials

# ====================
# SUPABASE CONFIGURATION
# ====================
# URL to your Supabase project (must start with https://)
# Find this in your Supabase Dashboard > Project Settings > API
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Public anon key (use the anon/public key from the Supabase dashboard)
# Find this in your Supabase Dashboard > Project Settings > API > Project API keys > anon key
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Service role key (⚠️ DO NOT expose this to browsers or commit to Git)
# Only use server-side. Find in Supabase Dashboard > Project Settings > API > service_role key
# VITE_SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ====================
# FIREBASE CONFIGURATION
# ====================
# Configuration Firebase pour l'authentification Google
# Find these in Firebase Console > Project Settings > General > Your apps
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id_here
VITE_FIREBASE_APP_ID=your_firebase_app_id_here

# ====================
# API SECRETS
# ====================
# Exhibitors fallback server secret
# ⚠️ IMPORTANT: Generate a strong random secret (min 32 characters)
# Use: openssl rand -hex 32
EXHIBITORS_SECRET=your_strong_random_secret_here_min_32_chars

# Metrics server secret
# ⚠️ IMPORTANT: Generate a different strong random secret
METRICS_SECRET=your_strong_random_secret_here_min_32_chars

# JWT Secret for authentication
# ⚠️ IMPORTANT: Generate a strong random secret (min 64 characters)
# Use: openssl rand -hex 64
JWT_SECRET=your_jwt_secret_here_min_64_chars

# ====================
# SERVER CONFIGURATION
# ====================
# Optional custom port for the exhibitors server (defaults to 4002)
EXHIBITORS_PORT=4002

# Frontend access to exhibitors fallback server
VITE_EXHIBITORS_SERVER_URL=http://localhost:4002

# ====================
# ALLOWED ORIGINS (CORS)
# ====================
# Comma-separated list of allowed origins for CORS
# Example: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=http://localhost:5000,http://localhost:3000

# Stripe (pour les paiements d'abonnements visiteurs)
# Clé publique Stripe (safe pour le frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
# Clé secrète Stripe (UNIQUEMENT côté serveur/Edge Functions)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
# Secret webhook Stripe (pour vérifier les webhooks)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ====================
# GOOGLE reCAPTCHA v3
# ====================
# Get your keys from: https://www.google.com/recaptcha/admin
# Register your site and choose reCAPTCHA v3

# Site key (public - used in frontend)
VITE_RECAPTCHA_SITE_KEY=6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI

# Secret key (private - used in backend/edge functions)
# ⚠️ NEVER expose this in frontend code
RECAPTCHA_SECRET_KEY=6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe

# Note: The keys above are Google's test keys that always pass validation
# Replace with your production keys before deploying to production
# Test keys are only for development and testing purposes

